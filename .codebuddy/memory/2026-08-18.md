# 2026-08-18

## 生成 docs/study/ 项目学习文档
- 用户要求：生成 docs/study 目录，完整展现项目（是什么/做什么/怎么做），深入代码每个细节，普通人能看懂，专业名词配网络链接。最后一步要求做"文档与代码一致性评审"。
- 产出 7 个文件：README.md、01-what-is-bindhosts.md、02-project-structure.md、03-module-scripts.md、04-webui-frontend.md、05-build-and-release.md、06-usage.md。
- 技术：Markdown + Mermaid 图 + 每个术语配官方/Wikipedia 链接 + 代码片段标 `文件:行号`。

### 一致性评审结论（code-explorer 子代理逐文件核对源码）
修正了 5 处偏差：
1. bindhosts.sh 函数清单补全 6 个真实函数（adaway_warn/stop_tcpdump/manager_install_zip/_month_txt2dec/_day_txt2dec/_is_valid_cron_arg）；删除杜撰的 `download`；`apply_description` 归 service.sh。
2. sources.txt 实为 13 行（前12行注释+第13行真实源），非"只有一行"。
3. repo.json 路径实为 `module/common/repo.json`。
4. more.js `reboot` 的 `svc power reboot` 实际在 util.js 第103-106 行的 reboot()，非 more.js 251。
5. 参数表确认有 --stop-tcpdump、无 --blacklist（规则页直接 sed/echo/rm 改文件）。

### 已正确标注的代码差异（非文档错误，待修正）
- more.js 的 issueTracker (570行) 与 localesUpdate (228行) 外链仍指向**原版** bindhosts/bindhosts，违反铁律4（应改 shiqianwei0508 仓库）。
- update.json 的 zipUrl 指向 releases/latest/download/（依赖 GitHub release），但本 fork 无 release 权限，实际 404；需改托管或手动传。

### 注意
- 这些文档尚未 git add/commit（用户未要求提交，且 plan mode 只是生成文档）。
- mode.sh 是运行时由 post-fs-data.sh 第114行生成，不在仓库手写文件（已澄清先前误解）。
